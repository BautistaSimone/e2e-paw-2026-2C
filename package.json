{
  "name": "paw-2026b-04-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Suite de E2E de Fuchibol. Vive fuera del repo entregable.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "tsx scripts/setup.ts",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "codegen": "tsx scripts/codegen.ts",
    "seed": "tsx scripts/seed.ts",
    "app": "tsx scripts/app.ts",
    "mailpit": "tsx scripts/mailpit.ts",
    "db:reset": "tsx scripts/db-reset.ts",
    "db:clean-dev": "tsx scripts/dev-clean.ts",
    "db:rebuild": "tsx scripts/db-rebuild.ts",
    "inspect": "tsx scripts/inspect.ts",
    "check:rules": "tsx scripts/check-rules.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "dotenv": "^16.4.7",
    "pg": "^8.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
